spring:
  datasource:
    url: jdbc:h2:mem:meddist;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

security:
  jwt:
    issuer: meddist-erp
    secret: test-secret-test-secret-test-secret
    access-token-ttl-minutes: 5
    refresh-token-ttl-days: 1
